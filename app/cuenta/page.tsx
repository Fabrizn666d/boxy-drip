import { RoutePlaceholder } from "@/components/ui/RoutePlaceholder";

export default function CuentaPage() {
  return <RoutePlaceholder eyebrow="Acceso / Próxima entrega" title="Mi cuenta" description="El acceso desde el navbar ya dirige a una ruta real. El flujo de autenticación se implementará en una fase posterior." />;
}
